spring.application.name=my-app

spring.cloud.aws.secretsmanager.enable=false
# spring.cloud.aws.region.static=ap-northeast-2
# spring.config.import=aws-secretsmanager:db_password

################################# for local testing #################################
# MySQL DB connection for testing (local)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/mydatabase?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=qwe1234

# API path for testing (local)
server.servlet.context-path=/api
################################# for local testing #################################

# Hibernate/JPA settings
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# JWT Secret Key (기존 환경변수나 외부 주입 방식 유지)
# spring.jwt.secret=${JWT_SECRET_KEY}
spring.jwt.secret=letitgoletitgocantholditbackanymoreletitgoletitgoturnawayandslamthedooridontcarewhattheyregoingtosay

# AWS S3 settings for local development
aws.s3.region=ap-northeast-2
aws.s3.bucket=mirum-test-bucket
