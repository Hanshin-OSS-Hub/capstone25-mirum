// Member data is primarily managed within the projects.json data, 
// as members are a subset/property of a project.
// We don't have a separate members.json. This file is a placeholder
// if we need specific member-related transformations in the future.
export {};
