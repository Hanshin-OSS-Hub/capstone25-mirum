/**
 * 파일 카테고리 Enum
 */
export const FILE_CATEGORY = Object.freeze({
  ALL: 'all',
  FOLDER: 'folder',
  MEDIA: 'media',
  DOCUMENT: 'document',
});

/**
 * 파일 뷰 모드 Enum
 */
export const FILE_VIEW_MODE = Object.freeze({
  LIST: 'list',
  GRID: 'grid',
});

/**
 * 폴더 가상 MIME 타입
 */
export const FOLDER_CONTENT_TYPE = 'application/x-folder';
